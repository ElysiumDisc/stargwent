# Runtime dependencies. Used at game runtime and bundled into the AppImage,
# .deb, .exe, and .dmg distributions. Pin minor versions for reproducibility.
pygame-ce>=2.5.0,<3
moderngl>=5.10,<6
Pillow>=10.0,<12
